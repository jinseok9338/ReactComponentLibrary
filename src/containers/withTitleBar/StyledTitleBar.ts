import styled from "styled-components";

//This is default setting for the TitleBar
const StyledTitleBar = styled.section`
  width: 100%;
  max-width: 100%;
  min-height: 30px;
  background: #32a89e;
  font-size: 12px;
  position: absolute;
  top: 0;
  left: 0;
`;

export default StyledTitleBar;
